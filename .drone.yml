---
kind: pipeline
type: ssh
name: backend

server:
  host: ci-runner1.nextpertise.nl
  user: runner
  ssh_key:
    from_secret: runner_rsa

steps:
- name: run tests
  commands:
  - poetry update
  - cd tests
  - poetry run pytest
  environment:
    DOCKER_CONFIG: /home/runner/.docker